# RPM packages installed in the tools Containerfile stage.
# Regenerate rpms.lock.yaml with: ./hack/update-rpms-lock.sh
packages:
- tar
- gzip
contentOrigin:
  repofiles:
  - ./hi.repo
arches:
- x86_64
- aarch64
context:
  containerfile:
    file: Containerfile
    stageName: tools
