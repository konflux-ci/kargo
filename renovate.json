{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "github>konflux-ci/mintmaker//config/renovate/renovate.json"
  ],
  "schedule": [
    "before 7am on Monday"
  ],
  "baseBranchPatterns": [
    "main",
    "release-1.10"
  ],
  "addLabels": [
    "konflux",
    "mintmaker"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^.tekton/.*\\.yaml$/"
      ],
      "matchStrings": ["KARGO_VERSION=(?<currentValue>v\\d+\\.\\d+\\.\\d+)"],
      "depNameTemplate": "akuity/kargo",
      "datasourceTemplate": "github-tags"
    }
  ],
  "packageRules": [
    {
      "description": "Go toolchain updates — deptriage defers approval until CI passes",
      "matchManagers": [
        "dockerfile"
      ],
      "matchPackageNames": [
        "/go-toolset/"
      ]
    },
    {
      "description": "Prioritize Konflux tekton task reference updates",
      "matchManagers": [
        "tekton"
      ],
      "prPriority": 10
    },
    {
      "description": "Never bump kargo across minor/major (main tracks git tip; release stays on patch line)",
      "matchDepNames": ["kargo", "akuity/kargo"],
      "matchUpdateTypes": ["major", "minor"],
      "enabled": false
    },
    {
      "description": "Group kargo patch updates",
      "matchDepNames": ["kargo", "akuity/kargo"],
      "matchUpdateTypes": ["patch"],
      "enabled": true,
      "groupName": "kargo-version"
    },
    {
      "description": "release-1.10: disable all updates (images, tekton refs, actions, etc.)",
      "matchBaseBranches": ["release-1.10"],
      "enabled": false
    },
    {
      "description": "release-1.10: only kargo 1.10.x patches (e.g. v1.10.7 → v1.10.8)",
      "matchBaseBranches": ["release-1.10"],
      "matchDepNames": ["kargo", "akuity/kargo"],
      "matchUpdateTypes": ["patch"],
      "allowedVersions": "/^v?1\\.10\\./",
      "enabled": true,
      "groupName": "kargo-version"
    }
  ]
}
